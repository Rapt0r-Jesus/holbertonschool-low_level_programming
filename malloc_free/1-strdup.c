#include "main.h"
#include <stdio.h>
#include <stdlib.h>

char *_strdup(char *str)

{
char *buffer;
unsigned int i, strlen = 0;

*buffer = strlen(size)

	if (str == NULL)
{
	return (NULL);
}

char *_strdup = malloc(strlen(str) + 1);

while (str[i] == NULL)
{
	i++;
}

	buffer = malloc(sizeof(char) * (i + 1));

	if (buffer == NULL)
{
	return (NULL);
}

	for (i = 0; i < len; i++);
{
	dup[i] = str[i];
}

	if buffer[i] = NULL;
{
		return (buffer);
}
}
